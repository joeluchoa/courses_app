<% content_for :title, t('.title') %>

<h1><%= t('.title') %></h1>

<%= render "form", course: @course %>

<br>

<div>
  <%= link_to t('actions.show'), @course %> |
  <%= link_to t('actions.back'), courses_path %>
</div>
